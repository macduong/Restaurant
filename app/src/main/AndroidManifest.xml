<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Activities.ManageATable"></activity>
        <activity android:name=".Activities.UpdateFood" />
        <activity android:name=".Activities.AddFood" />
        <activity android:name=".Activities.ManagerInfo" />
        <activity android:name=".Activities.ManageFood" />
        <activity android:name=".Activities.CreateTables" />
        <activity android:name=".Activities.ContinueManagerment" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>